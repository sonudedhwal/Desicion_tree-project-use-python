# Desicion_tree-project-use-python
DecisionTreeClassifier(): Creates the decision tree classifier.  model.fit(X, y): Trains the model using features X and labels y.  model.predict([[6, 82]]): Predicts the output for new input values.  print(...): Outputs the prediction result to the console.
